pipeline {
    agent any
    stages {
        stage('Hello') {
            steps {
                sh 'env'
                sh 'id'
                sh 'whoami'
                sh 'ls -a'
            }
        }
    }
